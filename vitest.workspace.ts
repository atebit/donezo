/**
 * Vitest workspace config — superseded by vitest.config.ts `projects`.
 *
 * In vitest 4, `defineWorkspace` was removed. Per-environment routing is now
 * handled via the `projects` array in vitest.config.ts. This file is kept as
 * a placeholder for documentation purposes.
 *
 * See vitest.config.ts for the active configuration.
 */
export {};
